interface:
  display_name: "陈硕 · Frontend Design"
  short_description: "陈硕的产品 UI 工作流：设计、实现、响应式检查与浏览器验证。"
  default_prompt: "使用 $frontend-design 把这个前端需求做成可用、精致、响应式并经过浏览器验证的产品界面。"

policy:
  allow_implicit_invocation: true
