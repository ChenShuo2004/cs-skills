interface:
  display_name: "Search Skill"
  short_description: "Deep competitor research with source-backed decisions."
  default_prompt: "Use $search-skill to research this market or competitor and produce a source-backed decision brief."

policy:
  allow_implicit_invocation: true
