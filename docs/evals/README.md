# 发布前真人验收

自动契约测试只能检查文档与边界是否存在，不能保证真实新会话中的路由、追问和交付质量。每个发布候选版本都复制 `v0.3.0.md`，使用**全新会话**逐项执行其中的 Prompt，并如实填入：

- 实际路由或动作；
- 关键输出或链接；
- 失败项与修复结论；
- 验收人和日期。

任一必填案例未通过时，不发布该版本。出现新的误路由、无意义追问、越权交付或不可用输出时，先把它加入 `tests/fixtures/` 的自动契约，或加入下一版真人验收案例。
