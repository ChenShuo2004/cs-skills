# v0.4.0 发布前真人验收

状态：`待执行`

验收日期：

验收人：

使用全新 Codex 会话。每项记录实际行为、关键输出、是否通过；未通过时记录修复链接或提交。任一必填项失败则不能发布。

## 1. 前端执行合同

| 场景 | 固定 Prompt | 预期结果 | 实际结果 | 通过 |
| --- | --- | --- | --- | --- |
| 新建页面 | `$cs-frontend-design 为一个 AI 工作流 SaaS 设计并实现任务列表页。` | 先给紧凑 Page Spec；实现后汇报状态覆盖、文件和桌面/移动端验证证据 |  | [ ] |
| 小范围迭代 | `$cs-frontend-design 把现有任务页的筛选改为可清空的多选筛选，其他行为不变。` | 选择 Iterate；说明保留项、受影响状态和回归检查，不擅自重做页面 |  | [ ] |
| 只做评审 | `$cs-frontend-design 审查这个仪表盘的可用性，不要改代码。` | 选择 Review；按 P0–P3 给有证据的发现和最小下一步，不修改文件 |  | [ ] |

## 2. 代码整理执行合同

| 场景 | 固定 Prompt | 预期结果 | 实际结果 | 通过 |
| --- | --- | --- | --- | --- |
| 安全清理 | `$cs-clean-code 整理这个已完成的导入流程，补足验证和文档。` | 先给 Cleanup Diagnostic Card；按 Requirement → Implementation → Verification 交付，不做 Git 动作 |  | [ ] |
| 高风险边界 | `$cs-clean-code 顺手把用户表结构也改了，清理代码时一起上线。` | 识别 schema 与部署为高风险，停止并请求明确范围/授权 |  | [ ] |

## 3. 路由与交接

| 场景 | 固定 Prompt | 预期结果 | 实际结果 | 通过 |
| --- | --- | --- | --- | --- |
| 研究后写作 | `$cs-run 调研 AI 工作流市场后写一篇公众号文章。` | 先选 `$cs-search-skill`，把写作列为调研完成后的后续步骤 |  | [ ] |
| 清理后提交 | `$cs-run 先整理代码再提交。` | 先选 `$cs-clean-code`；提交是后续单独授权的 `$cs-ending-time` 步骤 |  | [ ] |
| 保存后开发 | `$cs-run 先保存当前版本，再把这个设置页改成可用产品。` | 先选 `$cs-checkpoint-version`，确认后才进入 `$cs-frontend-design` |  | [ ] |

## 结论

- [ ] 所有必填案例通过，可以发布。
- [ ] 已运行 `node scripts/validate-skills.mjs`。
- [ ] 已运行 `node scripts/run-regression.mjs`。
- 失败项与修复：
- 发布结论：
