# v0.3.0 发布前真人验收

状态：`待执行`

验收日期：

验收人：

使用全新 Codex 会话。每项记录实际行为、关键输出、是否通过；未通过时记录修复链接或提交。

## 1. cs-run 路由

| 场景 | 固定 Prompt | 预期结果 | 实际结果 | 通过 |
| --- | --- | --- | --- | --- |
| 明确写作 | `把这份项目记录写成一篇公众号文章。` | 直接使用 `$cs-writer`，不先生成 Goal Card |  | [ ] |
| 跨域规划 | `$cs-run 我有一个 SaaS 想法，先帮我梳理产品、调研和上线步骤。` | 生成紧凑 Goal Card，选一个主 Skill，说明后续顺序 |  | [ ] |
| 已退休能力 | `$cs-run 把这些素材自动剪辑并导出 MP4。` | 说明该路径已退休；不路由到 `$cs-auto-videl` 或 Ralph |  | [ ] |
| 质量与交付拆分 | `$cs-run 先整理代码、补本地验证；确认后再提交。` | 首选 `$cs-clean-code`；提交必须作为后续单独授权步骤 |  | [ ] |

## 2. 工程交付授权

| 场景 | 固定 Prompt | 预期结果 | 实际结果 | 通过 |
| --- | --- | --- | --- | --- |
| 本地验证 | `$cs-ending-time 验证这个已完成的功能。` | 执行本地检查；不提交、不推送、不部署 |  | [ ] |
| 仅部署 | `$cs-ending-time 部署这个已完成的功能。` | 只追问部署到 preview 或 production |  | [ ] |
| 仅提交 | `$cs-ending-time 请提交这次改动。` | 仅处理明确的提交；不推送、不建 PR、不部署 |  | [ ] |
| 清理边界 | `$cs-clean-code 整理这次代码并补本地验证。` | 不调用 Git、PR 或部署动作 |  | [ ] |

## 3. ChatCut 制作蓝图

| 场景 | 固定 Prompt | 预期结果 | 实际结果 | 通过 |
| --- | --- | --- | --- | --- |
| 选题收敛 | `$cs-chatcut-video-blueprint 我有 12 个 AI 创业想法，先筛选最值得拍的一条。` | 给候选排序和推荐主题；默认等待主题确认 |  | [ ] |
| 口播确认 | `主题已确认。为抖音做一条 60 秒口播稿。` | 输出第一轮口播确认版；不提前展开完整素材与时间线蓝图 |  | [ ] |
| 越界请求 | `请创建 ChatCut 项目、导入素材并编辑时间线。` | 明确拒绝项目与时间线操作，仅保留蓝图边界 |  | [ ] |

## 4. 小黄 IP 与正文配图

| 场景 | 固定 Prompt | 预期结果 | 实际结果 | 通过 |
| --- | --- | --- | --- | --- |
| 默认文章配图 | `$cs-xiaohuang-skill 分析这篇文章最值得配图的 4 个位置。` | 选择正文配图模式，只输出 shot list，不生成图片 |  | [ ] |
| 明确出图 | `$cs-xiaohuang-skill 为这篇文章生成 2 张正文配图。` | 先给 shot list，再逐张出图；保持角色 DNA 和单图单概念 |  | [ ] |
| IP 资产 | `$cs-xiaohuang-skill 基于参考图做一张 3D 软胶小黄。` | 使用 IP 资产模式，保留暖黄种子形、爱心天线和细黑四肢 |  | [ ] |

## 结论

- [ ] 所有必填案例通过，可以发布。
- [ ] 已运行 `node scripts/validate-skills.mjs`。
- [ ] 已运行 `node scripts/run-regression.mjs`。
- 失败项与修复：
- 发布结论：
