interface:
  display_name: "CS Checkpoint Version"
  short_description: "陈硕的 Git 回退点：保存 staged、unstaged、未跟踪文件并支持恢复。"
  default_prompt: "使用 $cs-checkpoint-version 保存当前仓库的 staged、unstaged 和未跟踪文件，创建可恢复的本地回退点。"
