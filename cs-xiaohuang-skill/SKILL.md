---
name: cs-xiaohuang-skill
description: "当用户希望为中文文章、公众号、博客、Notion 文档、方法论、工作流或知识型帖子设计、生成或修改正文配图时使用：先识别文章中的认知锚点，再输出配图 shot list 或逐张生成 16:9 横版、白底、轻手绘的“小黄”正文配图。适用于文章插图、配图建议、单个观点可视化、去标题和局部改图；不用于商业 KV、PPT 信息图、复杂架构图、可编辑矢量图或 ChatCut 视频策划。"
metadata:
  author: "陈硕"
  collection: "CS Skills"
  source: "https://github.com/ChenShuo2004/cs-skills"
  compatibility: "Codex and any agent that supports SKILL.md"
---

# 小黄手绘正文配图

把中文内容中最值得理解的判断、变化、关系或隐喻，变成一张有记忆点、可穿插在正文中的 16:9 配图。目标是帮助读者一眼看懂关键动作，而不是把文字搬到图上。

## 先读参考

按任务需要读取，避免一次加载无关内容：

- `references/style-dna.md`：视觉语言、颜色和禁忌。
- `references/xiaohuang-ip.md`：小黄角色和动作规则。
- `references/composition-patterns.md`：构图选择与原创隐喻方法。
- `references/prompt-template.md`：单张生成和局部编辑模板。
- `references/qa-checklist.md`：生成后的检查与修正规则。

## 三种工作模式

- **配图规划**：用户要求分析文章怎么配图、哪些段落值得配图时，先输出 shot list，不生成图片。
- **直接生成**：用户明确要求生成、做图或输出图片时，为每个认知锚点单独调用 `image_gen`；不要把多张图拼成一张。
- **局部编辑**：用户提供图片并指定改动时，只改标题、错字、标注或指定元素；保持其余构图、比例与风格不变。

## 工作流程

1. **消化内容。** 读取用户提供的文章、笔记、链接、Markdown、Notion 内容、截图或单个观点；区分核心观点、认知转折、可视化关系和不需要配图的段落。
2. **选择认知锚点。** 不要平均配图。优先选择能帮助理解的判断、前后变化、输入输出、分流、瓶颈、反馈、角色状态或核心隐喻。
3. **先做配图策略。** 在未明确要求生成时，给出 3～6 张 shot list；短内容可为 1～3 张，长文通常不超过 8 张。每张包含：插入位置、核心意思、构图类型、小黄的动作、主要物件与建议标注词。
4. **发明新的隐喻。** 将抽象概念转换成可见动作和低科技物件；每次从当前文章重新设计画面，不复用之前的案例构图。
5. **逐张生成。** 每张图只表达一个结构。使用 `references/prompt-template.md` 组合提示词，并让小黄承担解释概念的关键动作。
6. **检查与迭代。** 按 `references/qa-checklist.md` 检查。中文错字多时，减少标注后重生成；元素过多时，删到只剩一个动作与少量物件。
7. **保存交付。** 当用户在 workspace 中工作时，将成图保存至 `assets/<article-slug>-illustrations/`，按 `01-topic.png`、`02-topic.png` 递增命名；不覆盖原图，除非用户明确要求。

## 硬规则

- 默认生成 16:9 横版正文配图，纯白背景、黑色轻手绘线条和充足留白。
- 小黄必须是画面的核心行动者，不得只是角落装饰；去掉小黄后画面核心动作应不成立。
- 一张图只解释一个认知动作或关系，正文标注要短，最多 6 处。
- 保持怪诞、轻松、克制和产品草图感；不要做成儿童插画、商业海报、课程课件或正式流程图。
- 不复制参考案例、他人 IP、旧图构图或原图中的受保护角色；为当前内容重新发明隐喻。
- 遇到需要品牌 KV、封面海报、PPT 信息图、复杂架构图或 ChatCut 视频时，明确说明不属于本 Skill 的边界。

## 输出口径

配图规划时，简短输出 shot list 和推荐优先级。生成完成后说明：生成数量、每张图服务的段落、文件路径，以及哪些图最适合保留。不要长篇解释视觉理论。
